---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import { Github, Linkedin, Instagram, Mail } from "lucide-astro";

import logo from "../assets/ysc_logo_no_background.png";

import tokenit from "../assets/projects/tokenit.png";
import tokenitApp from "../assets/projects/app-tokenit.png";
import zatobox from "../assets/projects/zatobox.png";
import consentido from "../assets/projects/consentido.png";

import elevate from "../assets/sideprojects/elevate.png";
import atlas from "../assets/sideprojects/atlas.png";
import aurora from "../assets/sideprojects/aurora.png";
import coffee from "../assets/sideprojects/coffee.png";

import arc1 from "../assets/projects/Arc/1.png";
import arc2 from "../assets/projects/Arc/2.png";
import arc3 from "../assets/projects/Arc/3.png";
import outdoor1 from "../assets/projects/outdoor/1.png";
import outdoor2 from "../assets/projects/outdoor/2.png";
import outdoor3 from "../assets/projects/outdoor/3.png";
import muebles1 from "../assets/projects/muebles/1.png";
import muebles2 from "../assets/projects/muebles/2.png";
import muebles3 from "../assets/projects/muebles/3.png";
import prisma1 from "../assets/projects/prisma/1.png";
import prisma2 from "../assets/projects/prisma/2.png";
import prisma3 from "../assets/projects/prisma/3.png";

const RAIL = [
  { label: "Intro", href: "#top" },
  { label: "Services", href: "#services" },
  { label: "Work", href: "#work" },
  { label: "Archive", href: "#side-projects" },
  { label: "Log", href: "#experience" },
  { label: "Stack", href: "#tech" },
  { label: "Pricing", href: "#pricing" },
  { label: "Contact", href: "#contact" },
];

const SERVICES = [
  { n: "01", title: "Landing Pages", desc: "High converting landing pages that turn visitors into customers." },
  { n: "02", title: "Business Websites", desc: "Professional websites that represent your brand and grow your business." },
  { n: "03", title: "Portfolio Websites", desc: "Showcase your work with elegant and modern portfolio websites." },
  { n: "04", title: "E-Commerce", desc: "Secure and scalable online stores with the best experience." },
  { n: "05", title: "Web Applications", desc: "Custom web apps built with the right tools for your needs." },
  { n: "06", title: "Maintenance", desc: "Keep your website updated, secure and running smoothly." },
];

const PRICING = [
  { n: "01", title: "Basic Landing", price: "199", original: "269", desc: "A focused one-pager to validate an idea or get a small business online fast — built right, nothing missing.", features: ["Up to 4 sections (hero, about, services, contact)", "Contact form wired to your email", "Responsive design", "Basic on-page SEO", "1 round of revisions", "1-3 days delivery"] },
  { n: "02", title: "Standard Landing", price: "299", original: "399", desc: "For businesses that need more room to explain themselves — more content, more trust signals, still lean.", features: ["Up to 7 sections (+ testimonials, FAQ, gallery)", "WhatsApp / social integrations", "Responsive design", "Full on-page SEO (meta tags, sitemap)", "2 rounds of revisions", "2-5 days delivery"], highlight: true },
  { n: "03", title: "Professional Landing", price: "409", original: "549", desc: "The complete package — built with the same care as this portfolio, tuned for conversion and performance.", features: ["Unlimited sections, tailored structure", "Custom motion design & interactions", "Performance & Core Web Vitals tuning", "Analytics setup (GA / Meta Pixel)", "3 rounds of revisions", "15 days of post-launch support"] },
];

const PROJECTS = [
  { n: "01", title: "Tokenit", cat: "Investor App · Web3", img: tokenitApp, alt: "Tokenit Investor App", desc: "Investor dashboard for a Chilean blockchain real-estate platform — real-time monitoring, KYC, digital signing and AI risk scoring.", tags: ["Meteor.js", "React", "Blockchain", "Web3"], url: "https://app.tokenit.cl" },
  { n: "02", title: "Tokenit", cat: "Landing Page · Web3", img: tokenit, alt: "Tokenit Landing", desc: "Public marketplace connecting investors with tokenized real-estate opportunities backed by real assets.", tags: ["Gatsby", "Python", "MongoDB"], url: "https://tokenit.cl" },
  { n: "03", title: "ZatoBox", cat: "Open Source · POS", img: zatobox, alt: "ZatoBox", desc: "Open-source point-of-sale system with real-time inventory, order management and a Docker-based deployment pipeline.", tags: ["Next.js", "TypeScript", "Supabase", "Docker"], url: "https://www.zatobox.io/" },
  { n: "04", title: "ConSentido Cognitivo", cat: "Psicopedagogía · Accessibility", img: consentido, alt: "ConSentido Cognitivo", desc: "Accessible platform for a psychopedagogy practice, built to make cognitive-support resources easy to reach for families.", tags: ["Next.js", "Tailwind CSS", "Firebase"], url: "https://www.consentidocognitivo.site/" },
];

const SIDE_PROJECTS = [
  { title: "Elevate Gym", img: elevate, alt: "Elevate Gym" },
  { title: "Atlas", img: atlas, alt: "Atlas" },
  { title: "Aurora AI", img: aurora, alt: "Aurora AI" },
  { title: "Noma Coffee", img: coffee, alt: "Noma Coffee" },
];

const REEL_PROJECTS = [
  { id: "arc", title: "Arc & Form", subtitle: "Interior Design Studio", desc: "A refined portfolio website for an interior design studio, built around large project imagery, editorial typography and a strong visual identity.", imgs: [arc1, arc2, arc3] },
  { id: "wild-oak", title: "Wild Oak Supply", subtitle: "Outdoor & Lifestyle Store", desc: "An e-commerce experience for a retailer selling outdoor equipment, apparel and everyday adventure gear, with product discovery and category-focused navigation.", imgs: [outdoor1, outdoor2, outdoor3] },
  { id: "muebles", title: "Muebles & Decoración", subtitle: "Furniture & Decor Store", desc: "A landing page and digital catalog for a furniture and home decor store, designed to showcase collections and featured products while making inquiries and orders easy.", imgs: [muebles1, muebles2, muebles3] },
  { id: "prisma", title: "Prisma Events", subtitle: "Event Production Company", desc: "A landing page for events and experiences, featuring upcoming dates, tickets, artists, galleries and a more experimental visual identity.", imgs: [prisma1, prisma2, prisma3] },
];

const TIMELINE = [
  { id: "codigo-facilito", year: "2026", type: "Education", title: "Codigo Facilito", role: "Modern Frontend Bootcamp", date: "Graduated Jan 2026", active: false },
  { id: "niuro", year: "2025", type: "Work", title: "Niuro", role: "Full Stack Engineer · Marketplace Platform", desc: "Placed at Tokenit, a Chilean blockchain-based real-estate investment platform. Real-time monitoring, KYC, digital signing and AI risk scoring.", date: "Dec 2025 — Present", active: true },
  { id: "zatobox-work", year: "2025", type: "Work", title: "ZatoBox", role: "Full Stack & Cloud Solutions Architect", desc: "DevOps Lead overseeing the full software development lifecycle — architecture from conception to deployment.", date: "Aug — Dec 2025", active: false },
  { id: "dragon-pyramid", year: "2025", type: "Work", title: "Dragon Pyramid", role: "Full Stack Engineer", desc: "Front-end focused work on the Gym Master project — Next.js 14, TailwindCSS, Shadcn UI, NextAuth, Supabase.", date: "Jun — Oct 2025", active: false },
  { id: "issd", year: "2025", type: "Education", title: "Inst. Sup. Santo Domingo", role: "Web Development & Mobile Applications", date: "2025 — Present", active: true },
  { id: "henry", year: "2025", type: "Education", title: "Henry", role: "Full Stack Development Bootcamp", date: "Graduated Jun 2025", active: false },
  { id: "coderhouse", year: "2025", type: "Education", title: "Coderhouse", role: "Full Stack Development Program", date: "Graduated Feb 2025", active: false },
  { id: "ysc", year: "2024", type: "Work", title: "YSC by frysccou", role: "Full Stack Engineer & Content Creator", desc: "Independent developer building functional, well-structured digital solutions and creating tech content.", date: "Aug 2024 — Present", active: true },
];

const TECH_STACK = [
  { id: "core-languages", label: "Core Languages", items: ["HTML", "CSS", "JavaScript", "TypeScript", "Java", "Python", "PHP"] },
  { id: "frontend", label: "Frontend", items: ["React", "Vue", "Angular", "Gatsby"] },
  { id: "backend", label: "Backend", items: ["Node.js", "NestJS", "Spring Boot", "Django", "FastAPI", "Laravel"] },
  { id: "meta-frameworks", label: "Meta-frameworks", items: ["Next.js", "Nuxt.js", "Meteor", "Astro"] },
  { id: "mobile", label: "Mobile", items: ["Flutter", "Dart", "React Native"] },
  { id: "styling-design", label: "Styling & Design", items: ["Tailwind CSS", "Bootstrap", "Figma", "Photoshop"] },
  { id: "databases", label: "Databases", items: ["MongoDB", "Supabase", "Firebase", "MySQL", "PostgreSQL", "Convex"] },
  { id: "tools-devops", label: "Tools & DevOps", items: ["Docker", "GitHub", "Git", "AWS"] },
];

const FOOTER_NAV = [
  { label: "Intro", href: "#top" },
  { label: "Work", href: "#work" },
  { label: "Archive", href: "#side-projects" },
  { label: "Log", href: "#experience" },
  { label: "Stack", href: "#tech" },
];
const FOOTER_SERVICES = SERVICES;

const BLACK = "#171522";
const BG = "#F5F3FA";
const VIOLET = "#6C4CCF";
const LINE = "rgba(23,21,34,.16)";

const STAMP = `<svg viewBox="0 0 60 60" width="100%" height="100%" fill="none" stroke="currentColor" stroke-width="1.1">
  <circle cx="30" cy="30" r="28" stroke-dasharray="2 3"/>
  <circle cx="30" cy="30" r="22"/>
  <path d="M30 10l3 5-3 4-3-4z" fill="currentColor" stroke="none"/>
  <text x="30" y="37" text-anchor="middle" font-size="17" fill="currentColor" stroke="none" font-family="'Zen Old Mincho', serif">結</text>
</svg>`;
---

<Layout>
  <div style={`color:${BLACK}; font-family:'Inter',sans-serif; min-height:100vh;`}>

    <!-- RAIL -->
    <nav class="rail" style={`position:fixed; left:0; top:0; bottom:0; width:56px; border-right:1px solid ${LINE}; z-index:60; display:flex; flex-direction:column; align-items:center; justify-content:space-between; padding:20px 0; background:${BG};`}>
      <a href="#top" style="display:flex;"><Image src={logo} alt="YSC" width={1774} height={887} style="width:48px; height:auto; object-fit:contain;" /></a>
      <div style="display:flex; flex-direction:column; align-items:center; gap:30px;">
        {RAIL.map(({ label, href }) => (
          <a href={href} data-rail={href} data-i18n={`nav.${href.slice(1)}`} class="rail-link" style={`writing-mode:vertical-rl; font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.14em; text-transform:uppercase; color:${BLACK}; opacity:.4; text-decoration:none; transition:opacity .2s,color .2s;`}>{label}</a>
        ))}
      </div>
      <div style="display:flex; flex-direction:column; align-items:center; gap:16px;">
        <button type="button" class="lang-toggle" aria-label="Switch language" style={`all:unset; cursor:pointer; box-sizing:border-box; width:26px; height:26px; display:flex; align-items:center; justify-content:center; font-family:'JetBrains Mono',monospace; font-weight:700; font-size:10px; letter-spacing:.04em; color:${BLACK}; border:1px solid ${BLACK};`}>ES</button>
        <div style={`width:20px; height:20px; color:${VIOLET};`} set:html={STAMP} />
      </div>
    </nav>

    <!-- TOP BAR -->
    <div class="topbar" style={`position:sticky; top:0; z-index:50; display:flex; align-items:center; gap:20px; height:52px; padding:0 clamp(20px,4vw,48px); background:rgba(245,243,250,.9); backdrop-filter:blur(10px); border-bottom:1px solid ${LINE};`}>
      <div class="mobile-nav" style="display:none; overflow-x:auto; gap:16px; flex:1;">
        {RAIL.map(({ label, href }) => (
          <a href={href} data-i18n={`nav.${href.slice(1)}`} style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:10.5px; letter-spacing:.08em; text-transform:uppercase; color:${BLACK}; opacity:.65; text-decoration:none; white-space:nowrap;`}>{label}</a>
        ))}
      </div>
      <button type="button" class="lang-toggle" aria-label="Switch language" style={`all:unset; cursor:pointer; box-sizing:border-box; width:28px; height:28px; display:flex; align-items:center; justify-content:center; flex-shrink:0; font-family:'JetBrains Mono',monospace; font-weight:700; font-size:10.5px; letter-spacing:.04em; color:${BLACK}; border:1px solid ${BLACK};`}>ES</button>
      <a href="#contact" class="avail-link" style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:11px; letter-spacing:.1em; text-transform:uppercase; color:${BLACK}; text-decoration:none; display:flex; align-items:center; gap:8px; white-space:nowrap; margin-left:auto;`}>
        <span style={`width:6px; height:6px; background:${VIOLET}; display:inline-block; flex-shrink:0;`}></span> <span class="avail-full" data-i18n="avail.full">Available for work — get in touch</span><span class="avail-short" data-i18n="avail.short" style="display:none;">Get in touch</span> ↗
      </a>
    </div>

    <div class="page-body">

    <!-- HERO -->
    <header id="top" class="r-pad" style={`position:relative; padding:clamp(64px,10vh,120px) clamp(24px,5vw,64px) clamp(40px,6vh,64px); border-bottom:1px solid ${LINE};`}>
      <div class="hero-glow"></div>
      <div style="overflow:hidden; margin-bottom:clamp(28px,5vh,56px); position:relative; z-index:1;">
        <div class="marquee" style="display:flex; width:max-content; font-family:'JetBrains Mono',monospace; font-size:12px; letter-spacing:.2em; text-transform:uppercase; white-space:nowrap;">
          <span style="display:inline-flex; gap:26px; padding-right:26px;">
            {Array.from({ length: 2 }).map(() => (
              <>
                <span style={`color:${VIOLET};`}>結</span><span data-i18n="hero.marquee.role">Full Stack Engineer</span><span style={`color:${VIOLET};`}>✦</span><span data-i18n="hero.marquee.location">Buenos Aires, AR</span><span style={`color:${VIOLET};`}>✦</span><span data-i18n="hero.marquee.est">Est. 2024</span><span style={`color:${VIOLET};`}>✦</span><span data-i18n="hero.marquee.available">Available for work</span><span style={`color:${VIOLET};`}>✦</span>
              </>
            ))}
          </span>
        </div>
      </div>

      <h1 class="r-h1" style={`font-family:'Poppins',sans-serif; font-weight:900; letter-spacing:-.03em; color:${BLACK}; line-height:.92;`}>
        <span style="display:block; font-size:clamp(38px,6.4vw,88px);" data-i18n="hero.h1.line1">Designing.</span>
        <span style="display:block; font-size:clamp(38px,6.4vw,88px); margin-left:clamp(0px,10vw,140px);" data-i18n="hero.h1.line2">Engineering.</span>
        <span style="display:block; font-size:clamp(38px,6.4vw,88px); margin-left:clamp(0px,4vw,60px);" data-i18n="hero.h1.line3prefix">Shipping</span>
        <span class="hero-tag" style={`display:block; font-family:'JetBrains Mono',monospace; font-weight:500; font-size:clamp(14px,1.6vw,18px); letter-spacing:.04em; text-transform:uppercase; color:rgba(23,21,34,.55); margin-left:clamp(0px,4vw,60px); margin-top:clamp(10px,1.6vh,16px);`}><span data-i18n="hero.h1.digital">digital</span> <span data-i18n="hero.h1.products">products.</span></span>
      </h1>

      <div class="hero-grid" style="display:grid; grid-template-columns:1.3fr .7fr; gap:clamp(24px,4vw,56px); margin-top:clamp(36px,6vh,64px); align-items:end;">
        <p style="max-width:54ch; font-size:16px; line-height:1.7; color:rgba(23,21,34,.6);" data-i18n="hero.intro">
          Francisco Espindola — Full Stack Engineer focused on building thoughtful digital products from end to end. Every project blends design, engineering and attention to detail to create experiences that feel simple, intuitive and built to last.
        </p>
        <div class="hero-btns" style="display:flex; flex-direction:column; gap:0;">
          <a href="#contact" class="btn-primary" data-i18n="hero.btn.start" style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:12.5px; letter-spacing:.06em; text-transform:uppercase; color:${BG}; text-decoration:none; background:${VIOLET}; padding:16px 24px; border:1px solid ${VIOLET}; text-align:center;`}>Start a project →</a>
          <a href="#work" class="btn-outline hero-btn-2" data-i18n="hero.btn.view" style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:12.5px; letter-spacing:.06em; text-transform:uppercase; color:${BLACK}; text-decoration:none; background:transparent; padding:16px 24px; border:1px solid ${BLACK}; border-top:none; text-align:center;`}>View the work</a>
        </div>
      </div>
    </header>

    <!-- SERVICES -->
    <section id="services" class="r-pad" style={`padding:clamp(56px,9vh,100px) clamp(24px,5vw,64px); border-bottom:1px solid ${LINE};`}>
      <div style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,48px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="services.header">Services — what I can do for you</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);">{SERVICES.length} <span data-i18n="services.count">disciplines</span></span>
      </div>

      <div style={`border-top:1px solid ${BLACK};`}>
        {SERVICES.map(({ n, title, desc }) => (
          <div class="menu-row" style={`border-bottom:1px solid ${LINE};`}>
            <div style="display:flex; align-items:center; gap:18px; padding:20px 4px; cursor:default;">
              <span style={`font-family:'JetBrains Mono',monospace; font-size:11px; color:${VIOLET}; flex-shrink:0;`}>{n}</span>
              <h3 class="menu-title" data-i18n={`services.${n}.title`} style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:clamp(20px,2.6vw,30px); color:${BLACK}; white-space:nowrap;`}>{title}</h3>
              <span class="leader" style={`flex:1; border-bottom:1px dotted rgba(23,21,34,.3); margin:0 4px; transform:translateY(-6px);`}></span>
            </div>
            <div class="menu-desc" style="max-height:0; overflow:hidden; transition:max-height .35s ease;">
              <p data-i18n={`services.${n}.desc`} style="font-size:13.5px; line-height:1.6; color:rgba(23,21,34,.55); padding:0 4px 20px 40px; max-width:56ch;">{desc}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- FEATURED WORK -->
    <section id="work" class="r-pad" style={`padding:clamp(56px,9vh,100px) clamp(24px,5vw,64px) 0; border-bottom:1px solid ${LINE};`}>
      <div style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,48px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="work.header">Work — selected exhibits</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);">{PROJECTS.length} <span data-i18n="work.count">case files</span></span>
      </div>

      {PROJECTS.map(({ n, title, cat, img, alt, desc, tags, url }, i) => (
        <article class="exhibit" style={`position:relative; display:grid; grid-template-columns:${i % 2 === 0 ? '1.1fr .9fr' : '.9fr 1.1fr'}; gap:clamp(24px,4vw,56px); align-items:center; padding:clamp(36px,6vh,64px) 0; border-bottom:1px solid ${LINE}; overflow:hidden;`}>
          <span aria-hidden="true" style={`position:absolute; top:-4vw; ${i % 2 === 0 ? 'right:-1vw' : 'left:-1vw'}; font-family:'Poppins',sans-serif; font-weight:900; font-size:clamp(120px,20vw,280px); line-height:1; color:${BLACK}; opacity:.04; pointer-events:none; z-index:0;`}>{n}</span>

          <div class="exhibit-media" style={`position:relative; z-index:1; order:${i % 2 === 0 ? 1 : 2}; border:1px solid ${LINE}; overflow:hidden;`}>
            <Image src={img} alt={alt} width={800} height={500} class="proj-img" style="width:100%; aspect-ratio:16/10; display:block; object-fit:cover; object-position:top; transition:transform .6s ease;" />
          </div>

          <div style={`position:relative; z-index:1; order:${i % 2 === 0 ? 2 : 1};`}>
            <div style={`font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.16em; text-transform:uppercase; color:${VIOLET}; margin-bottom:10px;`}><span data-i18n="work.exhibitLabel">Exhibit №</span>{n} — <span data-i18n={`work.${n}.cat`}>{cat}</span></div>
            <h3 style={`font-family:'Poppins',sans-serif; font-weight:800; font-size:clamp(28px,3.6vw,44px); color:${BLACK}; line-height:1; margin-bottom:16px;`}>{title}</h3>
            <p data-i18n={`work.${n}.desc`} style="font-size:14.5px; line-height:1.7; color:rgba(23,21,34,.6); max-width:48ch; margin-bottom:20px;">{desc}</p>
            <div style="display:flex; flex-wrap:wrap; gap:7px; margin-bottom:24px;">
              {tags.map(t => <span style={`font-family:'JetBrains Mono',monospace; font-size:10px; padding:4px 10px; border:1px solid ${LINE}; color:rgba(23,21,34,.55);`}>{t}</span>)}
            </div>
            <a href={url} target="_blank" rel="noopener noreferrer" class="exhibit-link" style={`display:inline-flex; align-items:center; gap:8px; font-family:'JetBrains Mono',monospace; font-weight:500; font-size:12px; letter-spacing:.08em; text-transform:uppercase; color:${BLACK}; text-decoration:none; border-bottom:1px solid ${BLACK}; padding-bottom:3px;`}><span data-i18n="work.openCase">Open case file</span> <span>↗</span></a>
          </div>
        </article>
      ))}
    </section>

    <!-- SIDE PROJECTS -->
    <section id="side-projects" style={`padding:clamp(56px,9vh,100px) 0; border-bottom:1px solid ${LINE};`}>
      <div class="r-pad" style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,44px); padding-left:clamp(24px,5vw,64px); padding-right:clamp(24px,5vw,64px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="archive.header">Archive — visual reel</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);" data-i18n="archive.scroll">Scroll to browse →</span>
      </div>

      <!-- REEL PROJECT CARDS -->
      <div class="r-pad reel-projects" style="display:grid; grid-template-columns:repeat(2,1fr); gap:24px; padding-left:clamp(24px,5vw,64px); padding-right:clamp(24px,5vw,64px); margin-bottom:clamp(36px,6vh,56px);">
        {REEL_PROJECTS.map(({ id, title, subtitle, desc, imgs }) => (
          <div class="reel-card" style={`border:1px solid ${LINE};`}>
            <div class="reel-slides" style="position:relative; aspect-ratio:16/10; overflow:hidden; background:rgba(23,21,34,.04);">
              {imgs.map((img, i) => (
                <Image src={img} alt={title} width={900} height={563} class="reel-slide" data-slide={i} style={`position:absolute; inset:0; width:100%; height:100%; object-fit:cover; opacity:${i === 0 ? 1 : 0}; transition:opacity .4s ease;`} />
              ))}
              <button type="button" class="reel-nav reel-prev" aria-label="Previous" style={`all:unset; cursor:pointer; position:absolute; left:8px; top:50%; transform:translateY(-50%); width:30px; height:30px; display:flex; align-items:center; justify-content:center; background:rgba(245,243,250,.85); color:${BLACK}; font-size:14px;`}>←</button>
              <button type="button" class="reel-nav reel-next" aria-label="Next" style={`all:unset; cursor:pointer; position:absolute; right:8px; top:50%; transform:translateY(-50%); width:30px; height:30px; display:flex; align-items:center; justify-content:center; background:rgba(245,243,250,.85); color:${BLACK}; font-size:14px;`}>→</button>
              <div class="reel-dots" style="position:absolute; bottom:10px; left:0; right:0; display:flex; justify-content:center; gap:6px;">
                {imgs.map((_, i) => (
                  <span class="reel-dot" data-dot={i} style={`width:6px; height:6px; border-radius:50%; background:${i === 0 ? BLACK : 'rgba(23,21,34,.3)'}; transition:background .25s ease;`}></span>
                ))}
              </div>
            </div>
            <div style={`padding:16px 18px; border-top:1px solid ${LINE};`}>
              <h3 style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:17px; color:${BLACK}; margin-bottom:2px;`}>{title}</h3>
              <div data-i18n={`reel.${id}.subtitle`} style={`font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.08em; text-transform:uppercase; color:${VIOLET}; margin-bottom:9px;`}>{subtitle}</div>
              <p data-i18n={`reel.${id}.desc`} style="font-size:12.5px; line-height:1.6; color:rgba(23,21,34,.6);">{desc}</p>
            </div>
          </div>
        ))}
      </div>

      <div class="reel" style="display:flex; justify-content:safe center; gap:16px; overflow-x:auto; padding:0 clamp(24px,5vw,64px) 12px; scroll-snap-type:x proximity;">
        {SIDE_PROJECTS.map(({ title, img, alt }, i) => (
          <button class="sp-card" data-index={i} style={`all:unset; cursor:pointer; flex:0 0 auto; width:min(72vw,300px); scroll-snap-align:start; border:1px solid ${LINE};`}>
            <div style="overflow:hidden;">
              <Image src={img} alt={alt} width={600} height={420} style="width:100%; aspect-ratio:4/3; display:block; object-fit:cover; filter:grayscale(1); transition:filter .5s ease, transform .5s ease;" class="sp-img" />
            </div>
            <div style={`padding:12px 14px; border-top:1px solid ${LINE}; display:flex; align-items:center; justify-content:space-between; gap:8px;`}>
              <span style={`font-family:'JetBrains Mono',monospace; font-size:10px; color:${VIOLET};`}>0{i + 1}</span>
              <h3 style={`font-family:'Poppins',sans-serif; font-weight:600; font-size:13.5px; color:${BLACK}; margin:0;`}>{title}</h3>
            </div>
          </button>
        ))}
      </div>
    </section>

    <!-- MODAL SIDE PROJECTS -->
    <div id="sp-modal" style="display:none; position:fixed; inset:0; z-index:200; background:rgba(12,9,10,.94); align-items:center; justify-content:center;">
      <button id="sp-close" style={`all:unset; cursor:pointer; position:absolute; top:22px; right:28px; z-index:10; font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.1em; text-transform:uppercase; color:${BG}; border:1px solid rgba(245,230,210,.35); padding:10px 18px;`}>✕ <span data-i18n="modal.close">Close</span></button>
      <button id="sp-prev" style={`all:unset; cursor:pointer; position:absolute; left:24px; top:50%; transform:translateY(-50%); z-index:10; width:44px; height:44px; border:1px solid rgba(245,230,210,.35); display:flex; align-items:center; justify-content:center; color:${BG}; font-size:18px;`}>←</button>
      <button id="sp-next" style={`all:unset; cursor:pointer; position:absolute; right:24px; top:50%; transform:translateY(-50%); z-index:10; width:44px; height:44px; border:1px solid rgba(245,230,210,.35); display:flex; align-items:center; justify-content:center; color:${BG}; font-size:18px;`}>→</button>

      <div style={`position:relative; width:min(90vw,760px); max-height:88vh; overflow-y:auto; border:1px solid rgba(245,230,210,.3); background:${BG};`}>
        <div style={`padding:20px 24px; border-bottom:1px solid ${LINE}; position:sticky; top:0; background:${BG}; z-index:5;`}>
          <div id="sp-modal-num" style={`font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.16em; text-transform:uppercase; color:${VIOLET}; margin-bottom:4px;`}></div>
          <h3 id="sp-modal-title" style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:22px; color:${BLACK}; margin:0;`}></h3>
        </div>
        <img id="sp-modal-img" src="" alt="" style="width:100%; display:block;" />
      </div>
    </div>

    <!-- EXPERIENCE / LOG -->
    <section id="experience" class="r-pad" style={`padding:clamp(56px,9vh,100px) clamp(24px,5vw,64px); border-bottom:1px solid ${LINE};`}>
      <div style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,48px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="log.header">Log — where I've been</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);" data-i18n="log.subtitle">Work &amp; education, reverse-chronological</span>
      </div>

      {Object.entries(
        TIMELINE.reduce((acc, entry) => {
          (acc[entry.year] ||= []).push(entry);
          return acc;
        }, {})
      ).sort((a, b) => b[0] - a[0]).map(([year, entries]) => (
        <div class="log-year" style={`display:grid; grid-template-columns:140px 1fr; gap:clamp(16px,3vw,32px); border-top:1px solid ${BLACK}; padding:clamp(20px,3vh,32px) 0;`}>
          <div style={`font-family:'Poppins',sans-serif; font-weight:900; font-size:clamp(40px,5vw,64px); color:${BLACK}; line-height:1;`}>{year}</div>
          <div>
            {entries.map(({ id, type, title, role, desc, date, active }) => (
              <div style="padding:14px 0; display:grid; grid-template-columns:auto 1fr; gap:16px; align-items:start;">
                <span data-i18n={`log.type.${type.toLowerCase()}`} style={`font-family:'JetBrains Mono',monospace; font-size:9px; letter-spacing:.1em; text-transform:uppercase; color:${VIOLET}; border:1px solid rgba(108,76,207,.4); padding:3px 8px; margin-top:3px; white-space:nowrap;`}>{type}</span>
                <div>
                  <div style="display:flex; align-items:center; gap:9px; flex-wrap:wrap; margin-bottom:3px;">
                    {active && <span style={`width:6px; height:6px; background:${VIOLET}; display:inline-block; border-radius:50%;`}></span>}
                    <h4 style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:16.5px; color:${BLACK};`}>{title}</h4>
                    <span data-i18n={`log.${id}.date`} style="font-family:'JetBrains Mono',monospace; font-size:10px; color:rgba(23,21,34,.4);">{date}</span>
                  </div>
                  <div data-i18n={`log.${id}.role`} style="font-family:'Inter',sans-serif; font-weight:600; font-size:12.5px; color:rgba(23,21,34,.6); margin-bottom:4px;">{role}</div>
                  {desc && <p data-i18n={`log.${id}.desc`} style="font-size:12.5px; line-height:1.6; color:rgba(23,21,34,.5); max-width:60ch;">{desc}</p>}
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </section>

    <!-- FULL TECH STACK -->
    <section id="tech" class="r-pad" style={`padding:clamp(56px,9vh,100px) clamp(24px,5vw,64px); border-bottom:1px solid ${LINE};`}>
      <div style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,44px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="tech.header">Stack — the full index</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);">{TECH_STACK.reduce((n, g) => n + g.items.length, 0)} <span data-i18n="tech.count">technologies</span></span>
      </div>

      <div style={`border-top:1px solid ${BLACK};`}>
        {TECH_STACK.map(({ id, label, items }) => (
          <div style={`padding:16px 0; border-bottom:1px solid ${LINE}; display:flex; flex-wrap:wrap; align-items:baseline; gap:4px 0;`}>
            <span data-i18n={`tech.label.${id}`} style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:13.5px; color:${BLACK}; width:190px; flex-shrink:0;`}>{label}</span>
            <span style="font-family:'JetBrains Mono',monospace; font-size:12.5px; line-height:2; color:rgba(23,21,34,.6);">
              {items.map((item, i) => (
                <>{item}{i < items.length - 1 && <span style={`color:${VIOLET};`}> · </span>}</>
              ))}
            </span>
          </div>
        ))}
      </div>
    </section>

    <!-- PRICING -->
    <section id="pricing" class="r-pad" style={`padding:clamp(56px,9vh,100px) clamp(24px,5vw,64px); border-bottom:1px solid ${LINE};`}>
      <div style="display:flex; align-items:baseline; justify-content:space-between; gap:24px; flex-wrap:wrap; margin-bottom:clamp(28px,5vh,48px);">
        <h2 style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET};`} data-i18n="pricing.header">Pricing — landing pages</h2>
        <span style="font-family:'JetBrains Mono',monospace; font-size:11px; color:rgba(23,21,34,.4);" data-i18n="pricing.usd">Prices in USD</span>
      </div>

      <div class="pricing-grid" style="display:grid; grid-template-columns:repeat(3,1fr); gap:0; border-top:1px solid #171522; border-left:1px solid #171522;">
        {PRICING.map(({ n, title, price, original, desc, features, highlight }) => {
          const discount = Math.round((1 - Number(price) / Number(original)) * 100);
          return (
            <div class="price-card" style={`position:relative; padding:clamp(24px,3vw,32px) clamp(20px,2.6vw,28px); border-right:1px solid ${LINE}; border-bottom:1px solid ${LINE}; ${highlight ? `background:${BLACK}; color:${BG};` : ''}`}>
              {highlight && <span data-i18n="pricing.badge" style={`position:absolute; top:0; right:0; font-family:'JetBrains Mono',monospace; font-size:9px; letter-spacing:.12em; text-transform:uppercase; color:${BG}; background:${VIOLET}; padding:5px 10px;`}>Most chosen</span>}
              <span style={`font-family:'JetBrains Mono',monospace; font-size:11px; color:${VIOLET};`}>{n}</span>
              <h3 data-i18n={`pricing.${n}.title`} style={`font-family:'Poppins',sans-serif; font-weight:700; font-size:clamp(19px,2.2vw,23px); margin:10px 0 4px; color:${highlight ? BG : BLACK};`}>{title}</h3>

              <div class="price-box" style="position:relative; height:clamp(34px,4vw,42px); margin-bottom:14px; cursor:pointer;">
                <div class="price-hidden" style="position:absolute; inset:0; display:flex; align-items:baseline; gap:8px; transition:opacity .25s ease;">
                  <span style={`font-family:'Poppins',sans-serif; font-weight:900; font-size:clamp(34px,4vw,42px); color:${highlight ? BG : BLACK};`}>??</span>
                  <span data-i18n="pricing.hoverHint" style={`font-family:'JetBrains Mono',monospace; font-size:10.5px; color:${highlight ? 'rgba(245,243,250,.5)' : 'rgba(23,21,34,.4)'};`}>Hover to see the price</span>
                </div>
                <div class="price-reveal" style="position:absolute; inset:0; display:flex; align-items:baseline; gap:8px; transition:opacity .25s ease;">
                  <span style={`font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.06em; color:${BG}; background:${VIOLET}; padding:2px 6px;`}>-{discount}%</span>
                  <span style={`font-family:'JetBrains Mono',monospace; font-size:13px; text-decoration:line-through; color:${highlight ? 'rgba(245,243,250,.45)' : 'rgba(23,21,34,.35)'};`}>${original}</span>
                  <span style={`font-family:'Poppins',sans-serif; font-weight:900; font-size:clamp(26px,3vw,32px); color:${highlight ? BG : BLACK};`}>${price}</span>
                </div>
              </div>

              <p data-i18n={`pricing.${n}.desc`} style={`font-size:13px; line-height:1.6; margin-bottom:18px; color:${highlight ? 'rgba(245,243,250,.65)' : 'rgba(23,21,34,.55)'};`}>{desc}</p>
              <ul style="list-style:none; display:flex; flex-direction:column; gap:8px;">
                {features.map((f, i) => (
                  <li style={`display:flex; align-items:center; gap:8px; font-family:'JetBrains Mono',monospace; font-size:11.5px; color:${highlight ? 'rgba(245,243,250,.8)' : 'rgba(23,21,34,.65)'};`}>
                    <span style={`color:${VIOLET};`}>✦</span> <span data-i18n={`pricing.${n}.feature.${i}`}>{f}</span>
                  </li>
                ))}
              </ul>
            </div>
          );
        })}
      </div>

      <p style="font-size:12.5px; line-height:1.7; color:rgba(23,21,34,.5); max-width:70ch; margin-top:20px;" data-i18n="pricing.note">
        Prices shown are for landing pages only. Business websites, e-commerce, web apps and other projects are quoted based on your specific needs — <a href="#contact" style={`color:${BLACK}; text-decoration:underline;`}>get in touch</a> for a quote. In every case, hosting, domain and any third-party services are paid directly by you — they are not included in the price.
      </p>
    </section>

    <!-- CONTACT / CTA -->
    <section id="contact" class="r-pad" style={`padding:clamp(64px,11vh,130px) clamp(24px,5vw,64px); background:${BLACK};`}>
      <div style="display:grid; grid-template-columns:1fr auto; gap:32px; align-items:end;">
        <div>
          <div style={`font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:.2em; text-transform:uppercase; color:${VIOLET}; margin-bottom:20px;`} data-i18n="contact.header">Contact — closing statement</div>
          <h2 style={`font-family:'Poppins',sans-serif; font-weight:900; font-size:clamp(34px,5.4vw,62px); line-height:1.02; letter-spacing:-.02em; color:${BG}; max-width:16ch; margin-bottom:24px;`} data-i18n="contact.h2">Let's build something worth remembering.</h2>
          <p style="max-width:50ch; font-size:15px; line-height:1.7; color:rgba(245,243,250,.6); margin-bottom:36px;" data-i18n="contact.paragraph">Open for freelance projects, collaborations and full-time opportunities. Tell me about your idea — we'll figure out scope and timing together.</p>
          <div style="display:inline-flex; flex-wrap:wrap;">
            <a href="mailto:franespindola71@gmail.com" data-i18n="contact.btn.email" style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:12.5px; letter-spacing:.06em; text-transform:uppercase; color:${BLACK}; text-decoration:none; background:${BG}; padding:17px 28px; border:1px solid ${BG};`}>Get in touch →</a>
            <a href="#work" data-i18n="contact.btn.work" style={`font-family:'JetBrains Mono',monospace; font-weight:500; font-size:12.5px; letter-spacing:.06em; text-transform:uppercase; color:${BG}; text-decoration:none; background:transparent; padding:17px 28px; border:1px solid rgba(245,243,250,.35); border-left:none;`}>Revisit the work</a>
          </div>
        </div>
        <div style={`width:clamp(70px,8vw,110px); height:clamp(70px,8vw,110px); color:${VIOLET}; flex-shrink:0;`} set:html={STAMP} />
      </div>
    </section>

    <!-- FOOTER -->
    <footer style={`background:${BLACK}; color:${BG};`}>
      <div class="r-pad" style={`padding:clamp(40px,6vh,56px) clamp(24px,5vw,64px) 0; border-bottom:1px solid rgba(245,243,250,.12);`}>
        <Image src={logo} alt="YSC by fryscou" width={1774} height={887} style="height:clamp(64px,9vw,120px); width:auto; object-fit:contain; display:block; filter:brightness(0) invert(1); margin-bottom:clamp(32px,5vh,48px);" />
      </div>
      <div class="r-pad footer-grid" style="padding:clamp(40px,6vh,56px) clamp(24px,5vw,64px); display:grid; grid-template-columns:1.4fr 1fr 1fr 1fr; gap:clamp(28px,4vw,48px);">
        <div>
          <p style="font-size:12.5px; line-height:1.7; color:rgba(245,243,250,.55); max-width:34ch;" data-i18n="footer.desc">Full Stack Engineer studio based in Buenos Aires, Argentina — building fast, elegant and reliable digital products end to end.</p>
        </div>
        <div>
          <div style="font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.16em; text-transform:uppercase; color:rgba(245,243,250,.4); margin-bottom:16px;" data-i18n="footer.navHeader">Navigation</div>
          <div style="display:flex; flex-direction:column; gap:10px;">
            {FOOTER_NAV.map(({ label, href }) => <a href={href} data-i18n={`nav.${href.slice(1)}`} class="footer-link" style="font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;">{label}</a>)}
          </div>
        </div>
        <div>
          <div style="font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.16em; text-transform:uppercase; color:rgba(245,243,250,.4); margin-bottom:16px;" data-i18n="footer.servicesHeader">Services</div>
          <div style="display:flex; flex-direction:column; gap:10px;">
            {FOOTER_SERVICES.map(s => <a href="#services" data-i18n={`services.${s.n}.title`} class="footer-link" style="font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;">{s.title}</a>)}
          </div>
        </div>
        <div>
          <div style="font-family:'JetBrains Mono',monospace; font-size:10px; letter-spacing:.16em; text-transform:uppercase; color:rgba(245,243,250,.4); margin-bottom:16px;" data-i18n="footer.elsewhereHeader">Elsewhere</div>
          <div style="display:flex; flex-direction:column; gap:12px;">
            <a href="https://github.com/esspindola" target="_blank" rel="noopener" class="footer-link" style="display:flex; align-items:center; gap:9px; font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;"><Github size={15} /> GitHub</a>
            <a href="https://www.linkedin.com/in/espindola-francisco" target="_blank" rel="noopener" class="footer-link" style="display:flex; align-items:center; gap:9px; font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;"><Linkedin size={15} /> LinkedIn</a>
            <a href="https://instagram.com/frysccou" target="_blank" rel="noopener" class="footer-link" style="display:flex; align-items:center; gap:9px; font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;"><Instagram size={15} /> Instagram</a>
            <a href="mailto:franespindola71@gmail.com" class="footer-link" style="display:flex; align-items:center; gap:9px; font-family:'Inter',sans-serif; font-size:13px; color:rgba(245,243,250,.75); text-decoration:none;"><Mail size={15} /> <span data-i18n="footer.email">Email</span></a>
          </div>
        </div>
      </div>
      <div class="r-pad" style="padding:18px clamp(24px,5vw,64px); border-top:1px solid rgba(245,243,250,.12); display:flex; flex-wrap:wrap; gap:12px; align-items:center; justify-content:space-between;">
        <span style="font-family:'JetBrains Mono',monospace; font-size:9.5px; letter-spacing:.1em; color:rgba(245,243,250,.4); text-transform:uppercase;">© 2026 YSC by fryscou — Buenos Aires, Argentina</span>
        <span style="font-family:'JetBrains Mono',monospace; font-size:9.5px; letter-spacing:.1em; color:rgba(245,243,250,.4); text-transform:uppercase;" data-i18n="footer.tagline">Designed &amp; built by Francisco Espindola</span>
      </div>
    </footer>

    </div>
  </div>
</Layout>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const hasFinePointer = window.matchMedia('(pointer: fine)').matches;

  const PROJECTS = [
    { title: "Elevate Gym", num: "01" },
    { title: "Atlas", num: "02" },
    { title: "Aurora AI", num: "03" },
    { title: "Noma Coffee", num: "04" },
  ];

  const modal = document.getElementById('sp-modal');
  const modalTitle = document.getElementById('sp-modal-title');
  const modalNum = document.getElementById('sp-modal-num');
  const modalImg = document.getElementById('sp-modal-img');
  const closeBtn = document.getElementById('sp-close');
  const prevBtn = document.getElementById('sp-prev');
  const nextBtn = document.getElementById('sp-next');

  let current = 0;
  const cards = document.querySelectorAll('.sp-card');
  const imgs = document.querySelectorAll('.sp-card img');

  function getImgSrc(index) {
    return imgs[index]?.src ?? '';
  }

  function openModal(index) {
    current = index;
    modalTitle.textContent = PROJECTS[index].title;
    modalNum.textContent = PROJECTS[index].num + ' / 04';
    modalImg.src = getImgSrc(index);
    modalImg.alt = PROJECTS[index].title;
    modal.style.display = 'flex';
    modal.querySelector('div[style*="overflow-y"]').scrollTop = 0;
    document.body.style.overflow = 'hidden';
  }

  function closeModal() {
    modal.style.display = 'none';
    document.body.style.overflow = '';
  }

  function navigate(dir) {
    current = (current + dir + PROJECTS.length) % PROJECTS.length;
    openModal(current);
  }

  cards.forEach((card, i) => {
    card.addEventListener('click', () => openModal(i));
  });

  closeBtn?.addEventListener('click', closeModal);
  prevBtn?.addEventListener('click', () => navigate(-1));
  nextBtn?.addEventListener('click', () => navigate(1));

  modal?.addEventListener('click', (e) => {
    if (e.target === modal) closeModal();
  });

  document.addEventListener('keydown', (e) => {
    if (modal.style.display === 'none') return;
    if (e.key === 'Escape') closeModal();
    if (e.key === 'ArrowLeft') navigate(-1);
    if (e.key === 'ArrowRight') navigate(1);
  });

  // reel project cards — manual carousel
  document.querySelectorAll('.reel-card').forEach((card) => {
    const slides = card.querySelectorAll('.reel-slide');
    const dots = card.querySelectorAll('.reel-dot');
    if (slides.length < 2) return;
    let active = 0;

    function show(index) {
      slides[active].style.opacity = '0';
      dots[active].style.background = 'rgba(23,21,34,.3)';
      active = (index + slides.length) % slides.length;
      slides[active].style.opacity = '1';
      dots[active].style.background = '#171522';
    }

    card.querySelector('.reel-prev')?.addEventListener('click', () => show(active - 1));
    card.querySelector('.reel-next')?.addEventListener('click', () => show(active + 1));
    dots.forEach((dot, i) => dot.addEventListener('click', () => show(i)));
  });

  // services accordion
  document.querySelectorAll('.menu-row').forEach(row => {
    const desc = row.querySelector('.menu-desc');
    row.addEventListener('mouseenter', () => { desc.style.maxHeight = desc.scrollHeight + 'px'; });
    row.addEventListener('mouseleave', () => { desc.style.maxHeight = '0'; });
  });

  // rail scrollspy
  const railLinks = document.querySelectorAll('.rail-link');
  const sections = Array.from(railLinks).map(l => document.querySelector(l.getAttribute('data-rail'))).filter(Boolean);
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const id = '#' + entry.target.id;
        railLinks.forEach(l => {
          const active = l.getAttribute('data-rail') === id;
          l.style.opacity = active ? '1' : '.4';
          l.style.color = active ? '#6C4CCF' : '#171522';
        });
      }
    });
  }, { rootMargin: '-40% 0px -50% 0px' });
  sections.forEach(s => observer.observe(s));

  // i18n — EN (default, baked into the markup) / ES toggle
  const ES = {
    'nav.top': 'Inicio', 'nav.work': 'Trabajos', 'nav.services': 'Servicios', 'nav.pricing': 'Precios',
    'nav.side-projects': 'Archivo', 'nav.experience': 'Trayectoria', 'nav.tech': 'Stack', 'nav.contact': 'Contacto',

    'avail.full': 'Disponible para trabajar — contáctame', 'avail.short': 'Contáctame',

    'hero.marquee.role': 'Full Stack Engineer', 'hero.marquee.location': 'Buenos Aires, AR',
    'hero.marquee.est': 'Desde 2024', 'hero.marquee.available': 'Disponible para trabajar',
    'hero.h1.line1': 'Diseñando.', 'hero.h1.line2': 'Ingeniando.', 'hero.h1.line3prefix': 'Lanzando',
    'hero.h1.digital': 'productos', 'hero.h1.products': 'digitales.',
    'hero.intro': 'Francisco Espíndola — Full Stack Engineer enfocado en construir productos digitales pensados de punta a punta. Cada proyecto combina diseño, ingeniería y atención al detalle para crear experiencias simples, intuitivas y hechas para durar.',
    'hero.btn.start': 'Iniciar un proyecto →', 'hero.btn.view': 'Ver los trabajos',

    'services.header': 'Servicios — en qué puedo ayudarte', 'services.count': 'disciplinas',
    'services.01.title': 'Landing Pages', 'services.01.desc': 'Landing pages de alta conversión que transforman visitantes en clientes.',
    'services.02.title': 'Sitios Web Empresariales', 'services.02.desc': 'Sitios web profesionales que representan tu marca y hacen crecer tu negocio.',
    'services.03.title': 'Sitios Portfolio', 'services.03.desc': 'Muestra tu trabajo con portfolios elegantes y modernos.',
    'services.04.title': 'E-Commerce', 'services.04.desc': 'Tiendas online seguras y escalables con la mejor experiencia de compra.',
    'services.05.title': 'Aplicaciones Web', 'services.05.desc': 'Aplicaciones web a medida, construidas con las herramientas correctas para tus necesidades.',
    'services.06.title': 'Mantenimiento', 'services.06.desc': 'Mantengo tu sitio actualizado, seguro y funcionando sin problemas.',

    'pricing.header': 'Precios — landing pages', 'pricing.usd': 'Precios en USD',
    'pricing.badge': 'Más elegido', 'pricing.currency': 'USD',
    'pricing.hoverHint': 'Pasa el cursor para ver el precio',
    'pricing.01.title': 'Landing Básica',
    'pricing.01.desc': 'Una landing de una sola página para validar una idea o poner en línea un negocio chico, rápido y bien hecha.',
    'pricing.01.feature.0': 'Hasta 4 secciones (hero, sobre mí, servicios, contacto)',
    'pricing.01.feature.1': 'Formulario de contacto conectado a tu email',
    'pricing.01.feature.2': 'Diseño responsive',
    'pricing.01.feature.3': 'SEO básico on-page',
    'pricing.01.feature.4': '1 ronda de revisiones',
    'pricing.01.feature.5': 'Entrega en 1-3 días',
    'pricing.02.title': 'Landing Standard',
    'pricing.02.desc': 'Para negocios que necesitan más espacio para explicarse — más contenido, más señales de confianza, sin perder foco.',
    'pricing.02.feature.0': 'Hasta 7 secciones (+ testimonios, FAQ, galería)',
    'pricing.02.feature.1': 'Integraciones con WhatsApp / redes sociales',
    'pricing.02.feature.2': 'Diseño responsive',
    'pricing.02.feature.3': 'SEO on-page completo (meta tags, sitemap)',
    'pricing.02.feature.4': '2 rondas de revisiones',
    'pricing.02.feature.5': 'Entrega en 2-5 días',
    'pricing.03.title': 'Landing Profesional',
    'pricing.03.desc': 'El paquete completo — construido con el mismo cuidado que este portfolio, optimizado para conversión y performance.',
    'pricing.03.feature.0': 'Secciones ilimitadas, estructura a medida',
    'pricing.03.feature.1': 'Motion design e interacciones a medida',
    'pricing.03.feature.2': 'Optimización de performance y Core Web Vitals',
    'pricing.03.feature.3': 'Configuración de analítica (GA / Meta Pixel)',
    'pricing.03.feature.4': '3 rondas de revisiones',
    'pricing.03.feature.5': '15 días de soporte post-lanzamiento',
    'pricing.note': 'Los precios mostrados son solo para landing pages. Sitios empresariales, e-commerce, aplicaciones web y otros proyectos se cotizan según tus necesidades específicas — <a href="#contact" style="color:#171522; text-decoration:underline;">contáctame</a> para una cotización. En todos los casos, el hosting, el dominio y cualquier servicio de terceros corren por tu cuenta — no están incluidos en el precio.',

    'work.header': 'Trabajos — casos destacados', 'work.count': 'casos de estudio', 'work.exhibitLabel': 'Expediente Nº',
    'work.01.cat': 'App de Inversores · Web3',
    'work.01.desc': 'Panel de inversores para una plataforma chilena de real estate en blockchain — monitoreo en tiempo real, KYC, firma digital y scoring de riesgo con IA.',
    'work.02.cat': 'Landing Page · Web3',
    'work.02.desc': 'Marketplace público que conecta inversores con oportunidades de real estate tokenizado respaldadas por activos reales.',
    'work.03.cat': 'Open Source · POS',
    'work.03.desc': 'Sistema de punto de venta open-source con inventario en tiempo real, gestión de pedidos y un pipeline de despliegue basado en Docker.',
    'work.04.cat': 'Psicopedagogía · Accesibilidad',
    'work.04.desc': 'Plataforma accesible para un consultorio de psicopedagogía, pensada para que las familias accedan fácilmente a recursos de apoyo cognitivo.',
    'work.openCase': 'Ver caso completo',

    'archive.header': 'Archivo — carrete visual', 'archive.scroll': 'Desliza para explorar →',
    'modal.close': 'Cerrar',

    'reel.arc.subtitle': 'Estudio de Diseño de Interiores',
    'reel.arc.desc': 'Un sitio portfolio refinado para un estudio de diseño de interiores, construido en torno a imágenes de proyectos a gran escala, tipografía editorial y una identidad visual sólida.',
    'reel.wild-oak.subtitle': 'Tienda Outdoor y Lifestyle',
    'reel.wild-oak.desc': 'Una experiencia de e-commerce para una tienda que vende equipo outdoor, indumentaria y gear de aventura para el día a día, con descubrimiento de productos y navegación enfocada en categorías.',
    'reel.muebles.subtitle': 'Tienda de Muebles y Decoración',
    'reel.muebles.desc': 'Landing y catálogo digital para una tienda de muebles y decoración, pensado para mostrar colecciones, productos destacados y facilitar consultas y pedidos.',
    'reel.prisma.subtitle': 'Productora de Eventos',
    'reel.prisma.desc': 'Landing para eventos y experiencias, con próximas fechas, entradas, artistas, galerías y una identidad visual más experimental.',

    'log.header': "Trayectoria — por dónde he pasado", 'log.subtitle': 'Trabajo y educación, en orden cronológico inverso',
    'log.type.education': 'Educación', 'log.type.work': 'Trabajo',
    'log.codigo-facilito.role': 'Bootcamp de Frontend Moderno', 'log.codigo-facilito.date': 'Graduado en enero de 2026',
    'log.niuro.role': 'Full Stack Engineer · Plataforma Marketplace',
    'log.niuro.desc': 'Asignado a Tokenit, una plataforma chilena de inversión inmobiliaria basada en blockchain. Monitoreo en tiempo real, KYC, firma digital y scoring de riesgo con IA.',
    'log.niuro.date': 'Dic 2025 — Presente',
    'log.zatobox-work.role': 'Full Stack & Cloud Solutions Architect',
    'log.zatobox-work.desc': 'Líder de DevOps a cargo de todo el ciclo de vida del desarrollo de software — arquitectura desde la concepción hasta el despliegue.',
    'log.zatobox-work.date': 'Ago — Dic 2025',
    'log.dragon-pyramid.role': 'Full Stack Engineer',
    'log.dragon-pyramid.desc': 'Trabajo enfocado en frontend para el proyecto Gym Master — Next.js 14, TailwindCSS, Shadcn UI, NextAuth, Supabase.',
    'log.dragon-pyramid.date': 'Jun — Oct 2025',
    'log.issd.role': 'Desarrollo Web y Aplicaciones Móviles', 'log.issd.date': '2025 — Presente',
    'log.henry.role': 'Bootcamp de Desarrollo Full Stack', 'log.henry.date': 'Graduado en junio de 2025',
    'log.coderhouse.role': 'Programa de Desarrollo Full Stack', 'log.coderhouse.date': 'Graduado en febrero de 2025',
    'log.ysc.role': 'Full Stack Engineer & Creador de Contenido',
    'log.ysc.desc': 'Desarrollador independiente construyendo soluciones digitales funcionales y bien estructuradas, y creando contenido tech.',
    'log.ysc.date': 'Ago 2024 — Presente',

    'tech.header': 'Stack — el índice completo', 'tech.count': 'tecnologías',
    'tech.label.core-languages': 'Lenguajes Core', 'tech.label.frontend': 'Frontend', 'tech.label.backend': 'Backend',
    'tech.label.meta-frameworks': 'Meta-frameworks', 'tech.label.mobile': 'Mobile',
    'tech.label.styling-design': 'Estilos y Diseño', 'tech.label.databases': 'Bases de Datos',
    'tech.label.tools-devops': 'Herramientas y DevOps',

    'contact.header': 'Contacto — cierre', 'contact.h2': 'Construyamos algo que valga la pena recordar.',
    'contact.paragraph': 'Disponible para proyectos freelance, colaboraciones y oportunidades full-time. Cuéntame tu idea — definimos juntos el alcance y los tiempos.',
    'contact.btn.email': 'Contáctame →', 'contact.btn.work': 'Volver a los trabajos',

    'footer.desc': 'Estudio de Full Stack Engineer con base en Buenos Aires, Argentina — construyendo productos digitales rápidos, elegantes y confiables de punta a punta.',
    'footer.navHeader': 'Navegación', 'footer.servicesHeader': 'Servicios', 'footer.elsewhereHeader': 'En otros lados',
    'footer.email': 'Correo', 'footer.tagline': 'Diseñado y desarrollado por Francisco Espíndola',
  };

  const LANG_KEY = 'site-lang';
  const originalHTML = new WeakMap();
  let currentLang = 'en';

  function applyLang(lang) {
    document.querySelectorAll('[data-i18n]').forEach((el) => {
      if (!originalHTML.has(el)) originalHTML.set(el, el.innerHTML);
      const key = el.getAttribute('data-i18n');
      const orig = originalHTML.get(el);
      el.innerHTML = (lang === 'es' && ES[key]) ? ES[key] : orig;
    });
    currentLang = lang;
    document.documentElement.lang = lang;
    document.querySelectorAll('.lang-toggle').forEach((btn) => {
      btn.textContent = lang === 'en' ? 'ES' : 'EN';
    });
    try { localStorage.setItem(LANG_KEY, lang); } catch {}
  }

  document.querySelectorAll('.lang-toggle').forEach((btn) => {
    btn.addEventListener('click', () => applyLang(currentLang === 'en' ? 'es' : 'en'));
  });

  let storedLang = 'en';
  try { storedLang = localStorage.getItem(LANG_KEY) || 'en'; } catch {}
  if (storedLang === 'es') applyLang('es');

  function splitWords(el) {
    const words = el.textContent.split(/(\s+)/);
    el.innerHTML = words.map((w) => (w.trim() ? `<span class="word"><span class="word-inner">${w}</span></span>` : w)).join('');
    return el.querySelectorAll('.word-inner');
  }

  if (!prefersReducedMotion) {
    gsap.from('#services', {
      clipPath: 'inset(0 100% 0 0)', opacity: 0, duration: 1.1, ease: 'power4.inOut',
      scrollTrigger: { trigger: '#services', start: 'top 82%' },
    });
    gsap.from('#work', {
      opacity: 0, y: 100, duration: 1, ease: 'power3.out',
      scrollTrigger: { trigger: '#work', start: 'top 85%' },
    });
    gsap.from('#side-projects', {
      opacity: 0, scale: 0.78, duration: 1, ease: 'back.out(1.4)',
      scrollTrigger: { trigger: '#side-projects', start: 'top 82%' },
    });
    gsap.from('#experience', {
      opacity: 0, x: -120, skewX: 6, duration: 1, ease: 'power4.out',
      scrollTrigger: { trigger: '#experience', start: 'top 82%' },
    });
    gsap.from('#tech', {
      opacity: 0, filter: 'blur(16px)', duration: 1.1, ease: 'power2.out',
      scrollTrigger: { trigger: '#tech', start: 'top 85%' },
    });
    gsap.from('#pricing', {
      clipPath: 'inset(0 0 0 100%)', opacity: 0, duration: 1.1, ease: 'power4.inOut',
      scrollTrigger: { trigger: '#pricing', start: 'top 82%' },
    });
    gsap.from('#contact', {
      opacity: 0, scale: 1.1, duration: 1.1, ease: 'power3.out',
      scrollTrigger: { trigger: '#contact', start: 'top 85%' },
    });
    gsap.from('footer', {
      opacity: 0, y: 50, duration: 0.9, ease: 'power2.out',
      scrollTrigger: { trigger: 'footer', start: 'top 92%' },
    });

    const heroLines = gsap.utils.toArray('.r-h1 > span:not(.hero-tag)');
    const heroWords = heroLines.flatMap((el) => Array.from(splitWords(el)));

    gsap.timeline({ defaults: { ease: 'power4.out' } })
      .from('.marquee', { opacity: 0, y: 20, duration: 0.6 })
      .from(heroWords, { opacity: 0, yPercent: 130, rotate: 6, stagger: 0.045, duration: 1 }, '-=0.25')
      .from('.hero-tag', { opacity: 0, y: 18, duration: 0.6 }, '-=0.5')
      .from('.hero-grid > *', { opacity: 0, y: 28, stagger: 0.12, duration: 0.7 }, '-=0.4')
      .from('.rail-link, .rail .lang-toggle, .rail > div:last-child > div:last-child', { opacity: 0, x: -12, stagger: 0.05, duration: 0.5 }, '-=0.9');

    gsap.utils.toArray('[data-i18n$=".header"]').forEach((el) => {
      const words = splitWords(el);
      gsap.from(words, {
        opacity: 0, yPercent: 100, stagger: 0.03, duration: 0.7, ease: 'power3.out',
        scrollTrigger: { trigger: el, start: 'top 85%' },
      });
    });

    gsap.utils.toArray('.exhibit').forEach((el, i) => {
      const dir = i % 2 === 0 ? -60 : 60;
      const img = el.querySelector('.proj-img');
      const tl = gsap.timeline({ scrollTrigger: { trigger: el, start: 'top 78%' } });
      tl.from(el, { opacity: 0, x: dir, rotate: i % 2 === 0 ? -1.5 : 1.5, duration: 1, ease: 'power4.out' });
      if (img) tl.from(img, { clipPath: 'inset(0 0 100% 0)', scale: 1.2, duration: 1.1, ease: 'power4.out' }, '<');
      tl.from(el.querySelectorAll('h3, p, div span'), { opacity: 0, y: 16, stagger: 0.04, duration: 0.6 }, '<0.15');

      const num = el.querySelector('span[aria-hidden="true"]');
      if (num) {
        gsap.fromTo(num, { yPercent: -16 }, {
          yPercent: 16, ease: 'none',
          scrollTrigger: { trigger: el, start: 'top bottom', end: 'bottom top', scrub: true },
        });
      }
    });

    ScrollTrigger.batch('.reel-card', {
      start: 'top 88%',
      onEnter: (batch) => gsap.from(batch, { opacity: 0, y: 60, scale: 0.92, rotate: -2, stagger: 0.12, duration: 0.9, ease: 'back.out(1.6)' }),
    });

    ScrollTrigger.batch('.sp-card', {
      start: 'top 92%',
      onEnter: (batch) => gsap.from(batch, { opacity: 0, y: 50, scale: 0.9, rotate: 3, stagger: 0.08, duration: 0.8, ease: 'back.out(1.7)' }),
    });

    gsap.utils.toArray('.log-year').forEach((el) => {
      gsap.from(el.children[0], { opacity: 0, x: -30, duration: 0.7, ease: 'power3.out', scrollTrigger: { trigger: el, start: 'top 88%' } });
      gsap.from(el.querySelectorAll(':scope > div:last-child > div'), {
        opacity: 0, y: 24, stagger: 0.08, duration: 0.6, ease: 'power2.out',
        scrollTrigger: { trigger: el, start: 'top 85%' },
      });
    });

    ScrollTrigger.batch('.price-card', {
      start: 'top 90%',
      onEnter: (batch) => gsap.from(batch, { opacity: 0, y: 50, scale: 0.94, rotate: -1, stagger: 0.1, duration: 0.8, ease: 'back.out(1.5)' }),
    });

    gsap.to('.hero-glow', {
      yPercent: 40, scale: 1.15, ease: 'none',
      scrollTrigger: { trigger: '#top', start: 'top top', end: 'bottom top', scrub: true },
    });
    gsap.to('.hero-glow', { scale: 1.08, opacity: 0.85, duration: 3.5, yoyo: true, repeat: -1, ease: 'sine.inOut' });

    gsap.utils.toArray('.exhibit-media, #contact > div > div:last-child').forEach((el) => {
      gsap.fromTo(el, { yPercent: -6 }, {
        yPercent: 6, ease: 'none',
        scrollTrigger: { trigger: el, start: 'top bottom', end: 'bottom top', scrub: true },
      });
    });
  }

  if (hasFinePointer && !prefersReducedMotion) {
    document.querySelectorAll('.sp-card, .reel-card').forEach((el) => {
      const rotateX = gsap.quickTo(el, 'rotationX', { duration: 0.4, ease: 'power3.out' });
      const rotateY = gsap.quickTo(el, 'rotationY', { duration: 0.4, ease: 'power3.out' });
      const lift = gsap.quickTo(el, 'z', { duration: 0.4, ease: 'power3.out' });

      el.addEventListener('mousemove', (e) => {
        const rect = el.getBoundingClientRect();
        const px = (e.clientX - rect.left) / rect.width - 0.5;
        const py = (e.clientY - rect.top) / rect.height - 0.5;
        rotateY(px * 16);
        rotateX(-py * 16);
        lift(24);
      });
      el.addEventListener('mouseleave', () => {
        rotateX(0);
        rotateY(0);
        lift(0);
      });
    });

    document.querySelectorAll('.btn-primary, .btn-outline, .exhibit-link, .lang-toggle').forEach((el) => {
      el.addEventListener('mousemove', (e) => {
        const r = el.getBoundingClientRect();
        gsap.to(el, { x: (e.clientX - r.left - r.width / 2) * 0.35, y: (e.clientY - r.top - r.height / 2) * 0.4, duration: 0.3, ease: 'power3.out' });
      });
      el.addEventListener('mouseleave', () => gsap.to(el, { x: 0, y: 0, duration: 0.6, ease: 'elastic.out(1,0.4)' }));
    });

  }
</script>

<style>
  @keyframes marquee-scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }
  .marquee { animation: marquee-scroll 22s linear infinite; }

  .page-body { margin-left: 56px; }
  .mobile-nav::-webkit-scrollbar { display: none; }

  .price-reveal { opacity: 0; }
  .price-box:hover .price-hidden { opacity: 0; }
  .price-box:hover .price-reveal { opacity: 1; }

  .reel, .reel-projects { perspective: 1200px; }
  .sp-card, .reel-card { transform-style: preserve-3d; will-change: transform; }

  .reel-nav, #sp-close, #sp-prev, #sp-next {
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #sp-modal > div {
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    background: rgba(245,243,250,.92) !important;
  }

  @media (prefers-reduced-motion: reduce) {
    .marquee { animation: none; }
  }

  .word { display: inline-block; overflow: hidden; vertical-align: top; }
  .word-inner { display: inline-block; }

  .exhibit-media { overflow: hidden; }

  .page-body { perspective: 1600px; }
  section, footer { transform-style: preserve-3d; will-change: transform; }

  @media (max-width: 900px) {
    .rail { display: none !important; }
    .page-body { margin-left: 0 !important; }
    .mobile-nav { display: flex !important; }
  }
  @media (max-width: 760px) {
    .pricing-grid { grid-template-columns: 1fr !important; }
    .reel-projects { grid-template-columns: 1fr !important; }
    .exhibit { grid-template-columns: 1fr !important; }
    .exhibit-media { order: 1 !important; }
    .exhibit > div:last-child { order: 2 !important; }
    .footer-grid { grid-template-columns: 1fr 1fr !important; }
    .hero-grid { grid-template-columns: 1fr !important; }
    .hero-btns { flex-direction: row !important; }
    .hero-btn-2 { border-top: 1px solid #171522 !important; border-left: none !important; }
    .log-year { grid-template-columns: 1fr !important; }
    .log-year > div:first-child { margin-bottom: 4px; }
  }
  @media (max-width: 600px) {
    .r-h1 span:not(.hero-tag) { margin-left: 0 !important; font-size: 34px !important; }
    .hero-tag { margin-left: 0 !important; }
    .r-pad { padding-left: 20px !important; padding-right: 20px !important; }
    .footer-grid { grid-template-columns: 1fr !important; }
    .mobile-nav { gap: 12px !important; }
  }
  @media (max-width: 480px) {
    .avail-full { display: none !important; }
    .avail-short { display: inline !important; }
    .menu-title { white-space: normal !important; font-size: 19px !important; }
    .leader { display: none !important; }
    .menu-row > div:first-child { flex-wrap: wrap; row-gap: 6px; }
  }
</style>
